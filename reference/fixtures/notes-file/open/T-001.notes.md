## 2026-09-20
Progress note fixture for WO-046 — shaped like land-branch.sh --no-complete's
own output (a dated heading plus free text, no frontmatter, no `id`), so the
fixture reproduces the exact input the loader must exclude.
